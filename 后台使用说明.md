# 作品集内容后台

双击桌面的“Port”即可使用。后台只运行在这台电脑上，外部访客无法打开或修改它。

## 日常更新

1. 在左侧选择要修改的模块。
2. 直接修改中英文标题、说明、封面和高清素材链接；图片可直接上传，系统会自动压缩。
3. 完成后点击右上角“保存并发布网站”。后台会安全提交 GitHub，Cloudflare Pages 会自动更新线上网站。

## 在手机上使用

1. 电脑与手机连接同一个 Wi-Fi，并保持 Port 开启。
2. 在电脑端 Port 的“总览”里查看“手机访问地址”和 6 位连接码。
3. 用手机浏览器打开该地址，输入连接码。
4. 在手机浏览器菜单中选择“添加到主屏幕”，以后可像 App 一样从 Port 图标进入。

GitHub 与 R2 密钥不会存入手机。手机上的编辑、图片压缩和网站发布都由电脑端 Port 安全执行。

## 视频上传（只需设置一次）

在“视频上传设置”中填入 Cloudflare R2 的 Account ID、Access Key ID 与 Secret Access Key。它们只储存在这台电脑的 `admin-private.json` 中，已被排除在 GitHub 之外。设置完成后，每张作品卡中的“上传视频到 R2”就可以直接把视频上传到你的 `jayden-portfolio-media` 存储桶。

## 注意

- “移除”只会让作品不在网页显示，不会删除本机原始素材或 R2 视频。
- 新增模块会使用现有作品展示页面的视觉排版。
- 若需要从 GitHub 获取别处更新，先点击“从 GitHub 更新”。
- 手机版需要电脑保持开机、Port 保持运行，并与手机处于同一局域网。
